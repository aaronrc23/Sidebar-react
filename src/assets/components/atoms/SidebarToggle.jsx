import styled from 'styled-components';

const SidebarToggle = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  color: #fff;
  margin: 10px;
  font-size: 1.5rem;
`;

export default SidebarToggle;
